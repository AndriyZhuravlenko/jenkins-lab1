---
- name: Configure Application Node
  hosts: app_node
  gather_facts: no
  tasks:
    - name: Bootstrap python for Ansible
      raw: apt-get update && apt-get install -y python3 python3-pip curl

    - name: Gather facts after bootstrap
      setup:

    - name: Install Docker Engine components
      apt:
        name:
          - docker.io
          - docker-compose
        state: present
        update_cache: yes

    - name: Ensure Docker service is running
      service:
        name: docker
        state: started

    - name: Run Node Exporter container
      docker_container:
        name: node-exporter
        image: prom/node-exporter:latest
        state: started
        restart_policy: always
        ports:
          - "9100:9100"

    - name: Run Web Application container (Nginx)
      docker_container:
        name: web-app
        image: nginx:latest
        state: started
        restart_policy: always
        ports:
          - "80:80"

- name: Configure Monitoring Node
  hosts: monitor_node
  gather_facts: no
  vars:
    # Цю змінну ми передамо через Jenkins з outputs Terraform
    app_node_ip: "{{ hostvars['localhost']['terraform_app_ip'] }}"
  tasks:
    - name: Bootstrap python for Ansible
      raw: apt-get update && apt-get install -y python3 python3-pip curl

    - name: Gather facts after bootstrap
      setup:

    - name: Install Docker Engine components
      apt:
        name:
          - docker.io
          - docker-compose
        state: present
        update_cache: yes

    - name: Ensure Docker service is running
      service:
        name: docker
        state: started

    - name: Create Prometheus configuration directory
      file:
        path: /etc/prometheus
        state: directory
        mode: '0755'

    - name: Generate Prometheus configuration from Jinja2 template
      template:
        src: templates/prometheus.yml.j2
        dest: /etc/prometheus/prometheus.yml
        mode: '0644'

    - name: Run Prometheus container
      docker_container:
        name: prometheus
        image: prom/prometheus:latest
        state: started
        restart_policy: always
        ports:
          - "9090:9090"
        volumes:
          - /etc/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml

    - name: Run Grafana container
      docker_container:
        name: grafana
        image: grafana/grafana:latest
        state: started
        restart_policy: always
        ports:
          - "3000:3000"
