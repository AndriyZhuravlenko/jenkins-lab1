---
- name: Deploy Containerized App on multi-node infrastructure
  hosts: web_servers
  gather_facts: no
  tasks:
    - name: Update apt cache and install Nginx inside containers
      raw: apt-get update && apt-get install -y nginx

    - name: Ensure Nginx is reloaded and active
      raw: nginx -s reload
